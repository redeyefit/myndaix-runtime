{
  "summary": "Adversarial multi-lens verification of the controller-loop before merge: confirm prior findings closed + hunt residual bugs, each finding verified by skeptics",
  "agentCount": 15,
  "logs": [],
  "result": {
    "confirmed_count": 6,
    "confirmed": [
      {
        "severity": "MINOR",
        "title": "Daily-budget break skips the (free) advance pass for all later repos, delaying their cursor advance up to a full UTC day",
        "location": "src/runtime/controller.py:432-434",
        "why_real": "The for-loop processes repos in a FIXED config order and evaluates `if not DRY_RUN and _day_count() >= MAX_DISPATCH_PER_DAY: break` at the TOP of each iteration, before calling process_repo. The advance pass (lines 328-337) lives inside process_repo and consumes NO dispatch budget (it only reads done-<sha> and runs advance_cursor). Scenario: repo #1 is busy and consumes the entire MAX_DISPATCH_PER_DAY=20 budget for the day; repo #2 had a review that already DELIVERED (done-<sha> present) and is sitting at pending_sha set. Because the loop breaks before reaching repo #2, repo #2's cursor never advances off its pending head until the next UTC day resets the counter. Not a permanent wedge (recovers next day), but a real avoidable stall of free bookkeeping work and of repo fairness.",
        "fix": "Make the advance pass independent of the dispatch budget: either run the advance pass for every repo first (a separate loop, since it dispatches nothing), or move the daily-budget gate to wrap only the dispatch portion inside process_repo (e.g. pass the day-budget-exhausted flag in and let the advance pass still run, returning before claim_dispatch) instead of `break`-ing the whole loop.",
        "lens": "state-machine",
        "verdict": {
          "real": true,
          "reasoning": "REAL bug, correctly characterized as MINOR. I traced the full path on branch northstar/controller-loop and every link holds.\n\nCONCRETE FAILING PATH:\n- The advance pass (src/runtime/controller.py:328-337) is the ONLY code anywhere that calls advance_cursor (grep confirms: no other caller in src/ or orchestrator/, only play-review's done-<sha> marker write feeds it). It runs at the TOP of process_repo, consumes ZERO dispatch budget (it only reads the done-<sha> file via _done(), runs git update-ref via _pin(), and calls advance_cursor — never trigger_review/_charge_day).\n- The daily-budget gate at lines 433-434 is `if not DRY_RUN and _day_count() >= MAX_DISPATCH_PER_DAY: break`, evaluated at the TOP of each loop iteration, BEFORE process_repo is called. `break` exits the whole `for repo in repos` loop.\n- _day_count() (lines 403-407) reads the persistent UTC-day-keyed file `controller-day-<utcday>` (line 400). It is only incremented by _charge_day() on a successful trigger_review (line 392) and only resets when the UTC date string rolls over. So once exhausted it stays exhausted for the rest of the UTC day across every hourly tick.\n\nSCENARIO: Within one UTC day a busy repo consumes the full MAX_DISPATCH_PER_DAY=20 budget. A second (config-ordered-later) repo has a prior dispatch whose review already DELIVERED — cursor has pending_sha=X and the done-X marker is present (the deliver-then-advance timing is necessarily on a later tick, since play-review's FRONT detaches the worker and exits fast, line 299, so the done marker is written after the dispatching tick exits). On the next tick: line 433 sees _day_count()>=20 at the FIRST repo and breaks, so process_repo is never called for the second repo, so its advance pass never runs, so advance_cursor is never invoked. The cursor stays pinned at the stale reviewed_sha with pending_sha=X. This repeats every tick until the UTC counter resets (~up to a full day).\n\nCONSEQUENCE IS REAL, NOT COSMETIC: until the advance pass runs, claim_dispatch (postgres_store.py:884-912) cannot dispatch any NEWER head for that repo (a fresh in-flight pending is not superseded; reviewed_sha is stale), so the repo's review pipeline is stalled for the remainder of the UTC day even though the advance is free, non-budget work. Self-heals next UTC day (the advance pass runs first in process_repo, then dispatch can proceed), so not a permanent wedge — hence MINOR.\n\nDESIGN-INTENT CHECK confirms this is a deviation, not by-design: docs/controller-loop-design.md line 81 gates the loop on the dispatch budget, but line 96 describes ADVANCE as a distinct downstream phase tied to delivery confirmation ('on a later tick'), and the runaway-dispatch row (line 117) scopes the daily budget to DISPATCH, not to the advance/bookkeeping pass. Coupling the free advance pass to the daily DISPATCH budget via a single whole-loop `break` is an implementation deviation.\n\nSkeptical objections all fail: (1) one repo can plausibly exhaust 20 dispatches/day across hourly ticks (3/tick cap x 24); (2) the deliver-then-advance window genuinely spans ticks; (3) repo order is fixed (load_config iterates raw.items() insertion order, stable each tick); (4) the stall has real forward-progress consequences, not just observability. Note: the break actually skips ALL repos' advance passes once budget is exhausted (even the busy repo's own), which is broader than but consistent with the reviewer's claim. The proposed fix (decouple advance from dispatch budget — separate pre-loop, or move the gate to wrap only the dispatch portion inside process_repo) is correct.\n\nRelevant file: /Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/controller.py (lines 322-337 advance pass; 432-434 the break; 398-415 day counter)."
        }
      },
      {
        "severity": "MINOR",
        "title": "Stale code comments reference the deleted PLAY_FORCE_DONE flag as the mechanism that makes the advance marker branch-move-proof",
        "location": "src/runtime/controller.py:327 (also 130, 277)",
        "why_real": "Per the design v0.5 changelog, PLAY_FORCE_DONE was DELETED and the controller now relies on passing an EMPTY remote URL so play-review's confirm_pushed returns 0 (pushed=1) and mark_done writes done-<sha>. _review_env() correctly sets only PLAY_DISABLE_AUTOFIX and never PLAY_FORCE_DONE, and the empty-URL trigger is correct, so behavior is fine. But the advance-pass comment at line 327 still asserts the marker is 'made branch-move-proof by PLAY_FORCE_DONE', which no longer exists. This is misleading for the next maintainer reasoning about whether the cursor can advance — it points at a non-existent guarantee. Functionally a no-op (the empty-URL path does write done-<sha>), so MINOR/doc-drift, not a logic bug.",
        "fix": "Update the comments at controller.py:327 (and the lingering mention at 130/277) to describe the actual mechanism: empty remote URL -> confirm_pushed returns 0 -> pushed=1 -> mark_done writes the post-delivery done-<sha> marker. Drop the PLAY_FORCE_DONE reference.",
        "lens": "state-machine",
        "verdict": {
          "real": true,
          "reasoning": "Verified on branch northstar/controller-loop. The claim is accurate and scoped honestly as MINOR doc-drift (the reviewer explicitly says it is a functional no-op, NOT a logic bug), and within that scope it genuinely manifests in the current code.\n\nFACTS CONFIRMED:\n- PLAY_FORCE_DONE is DELETED — it exists nowhere in live code. grep across the repo shows it only in: (a) the design doc v0.4 changelog (controller-loop-design.md:16, the now-superseded entry), (b) the v0.5 changelog (line 8) which explicitly announces \"PLAY_FORCE_DONE DELETED ... controller now passes an empty remote URL\", (c) historical v0.4 review docs, and (d) the two controller.py comments cited.\n- The ACTUAL mechanism is the empty-URL path, verified end to end: controller.py:296 passes argv [\"origin\", \"\"] -> play-review FRONT remote_url=\"${2:-}\" (empty) -> forwarded to worker remote_url=\"${6:-}\" (empty) -> confirm_pushed() at play-review.sh:128-129 `[[ -z \"$remote_url\" ]] && return 0` -> main flow line 280 sets pushed=1 -> mark_done() line 148 writes $STATE/done-$tip. _review_env() sets only PLAY_DISABLE_AUTOFIX, never PLAY_FORCE_DONE. So behavior is correct; this is purely a comment defect.\n\nPER-LOCATION:\n- controller.py:327 — \"done-<sha> marker, made branch-move-proof by PLAY_FORCE_DONE) moves the cursor.\" This is the strongest, real instance: it asserts a CURRENT positive guarantee (\"made branch-move-proof by PLAY_FORCE_DONE\") attached to a flag that no longer exists. The phrasing was copied verbatim from the superseded v0.4 changelog (design doc line 16). A maintainer reasoning about cursor advancement here would search for PLAY_FORCE_DONE, find only historical review docs, and be misled about the actual mechanism. Genuinely stale and misleading in code-as-written.\n- controller.py:130 and trigger_review docstring (line ~277) — say \"WITHOUT a public PLAY_FORCE_DONE bypass\" / \"no public force-done bypass.\" These do NOT misrepresent the mechanism (they correctly describe the empty-URL path and the flag's ABSENCE); they only lingeringly mention the deleted name. The reviewer correctly characterizes these as \"lingering mention(s),\" not as the misleading assertion.\n\nCONCLUSION: The reviewer did not overstate this as a logic bug; they scoped it correctly as MINOR doc-drift, and the cited line 327 comment does exist, does reference the deleted flag as the live branch-move-proof mechanism, and is misleading to a future maintainer. The proposed fix (rewrite the comments to describe empty-URL -> confirm_pushed returns 0 -> pushed=1 -> mark_done writes done-<sha>, dropping the PLAY_FORCE_DONE reference) is correct. Zero behavioral risk, but it is a real, current code-comment defect, not a historical/already-fixed one.\n\nRelevant files (absolute): /Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/controller.py (lines 327, 130, ~277), /Users/stevenfernandez/code/active/myndaix-runtime/orchestrator/play-review.sh (confirm_pushed 128-142, mark_done 148, FRONT 44-69), /Users/stevenfernandez/code/active/myndaix-runtime/docs/controller-loop-design.md (v0.5 changelog line 8 = current; v0.4 line 16 = source of the stale phrasing)."
        }
      },
      {
        "severity": "MAJOR",
        "title": "Baseline seed ignores the reviewed-pin result, so a failed pin can wedge cat-file forever (silently)",
        "location": "src/runtime/controller.py:357-360",
        "why_real": "First-sight path: `if await led.upsert_baseline(...): _pin(repo, _ctl_reviewed_ref(ref), head)` — the `_pin` return value is discarded, unlike the advance path (lines 333-336) which explicitly refuses to advance if the pin fails because an unpinned base 'would wedge cat-file'. `_pin` runs `git update-ref` which can return nonzero on transient failures (concurrent gc/pack-refs holding the ref lock, packed-refs lock contention, FS error). If upsert_baseline succeeds (cursor row created, reviewed_sha=head=H0) but the pin write fails, H0 has NO controller-owned ref anchoring it. With the default core.logAllRefUpdates=true (confirmed: reflogs are auto-created only for refs/heads, refs/remotes, refs/notes, HEAD — NOT refs/myndaix/*), there is also no reflog fallback for H0. If the remote then force-pushes H0 away and local `git gc` prunes it, every subsequent tick computes base=reviewed_sha=H0 and hits the cat-file gate at lines 374-376 ('head/base objects not present locally — skip') and returns BEFORE claim_dispatch. Because it bails before claiming, attempts never increment and mark_blocked never fires — the repo is silently skipped on every tick forever, with no 'blocked' surfacing. The baseline pin is never retried (cur is no longer None), and the base is never re-fetched (the fetch refspec only pulls the branch tip), so there is no self-healing. This is exactly the gc-prune-wedge the design guards against in the advance path, left unguarded at the point reviewed_sha is first established.",
        "fix": "Guard the baseline pin the same way the advance path does: only seed the cursor if the pin succeeds, e.g. pin first and abort the seed if it fails — `if _pin(repo, _ctl_reviewed_ref(ref), head) and await led.upsert_baseline(rid, ref, head): log(...seeded...)` else log a refusal and retry next tick (head is still reachable via the just-fetched head ref, so re-fetch/re-pin will succeed). Additionally, make the dispatch-path cat-file gate at 374-376 fail-loud rather than silent: if base^{commit} is missing, treat it as a blocked/surfaced condition (or attempt a targeted re-fetch of base) instead of a bare return, so a wedged base is alerted rather than skipped silently forever.",
        "lens": "git-gc",
        "verdict": {
          "real": true,
          "reasoning": "VERDICT: REAL (but low-probability / narrow-precondition), MAJOR is generous — this is a genuine latent defect in the code as written, not a historical/fixed one. I verified every link in the reviewer's chain against the current code on branch northstar/controller-loop.\n\nCODE FACTS — all confirmed:\n1. The asymmetry is real. src/runtime/controller.py:357-359 (seed path) discards the `_pin` return: `if await led.upsert_baseline(...): _pin(repo, _ctl_reviewed_ref(ref), head); log(...)`. The advance path at 333-336 explicitly checks it and refuses to advance onto an unpinnable base (\"would wedge cat-file\"). `_pin` (315-319) is literally `git update-ref(...).returncode == 0`, so its failure is silently dropped at seed but honored at advance. The authors demonstrably know an unpinned reviewed_sha wedges cat-file, yet guard only one of the two write sites.\n2. refs/myndaix/* gets NO reflog by default — verified empirically (git update-ref into refs/myndaix/reviewed/* creates no .git/logs entry; default core.logAllRefUpdates logs only heads/remotes/notes/HEAD). The controller's own fetch into refs/myndaix/controller/* also writes no reflog (verified: .git/logs/refs/myndaix/ never created). So neither controller-owned ref leaves a reflog trail for H0.\n3. The cat-file gate at 374-376 fires on a missing base BEFORE claim_dispatch (382). Therefore attempts never increments, the blocked ceiling at 365 (which keys on pending_sha==head AND attempts>=MAX) is never reached, mark_blocked never fires, no verdict is ever surfaced to the jefe inbox. The fetch refspec (344-345) only pulls the branch tip into the owned head ref and never re-fetches base, and since cur is no longer None the seed/pin is never retried — so there is NO self-healing. The wedge is permanent, emitting only a recurring \"head/base objects not present locally — skip\" log line on every tick. This matches the reviewer's description exactly (it is not strictly silent — there is a log line — but it never escalates/surfaces and never recovers).\n\nCONCRETE FAILING PATH (constructed): (a) at first sight the reviewed-pin git update-ref fails transiently (a concurrent developer-run or auto `git gc`/`git pack-refs` holding the loose-ref/packed-refs lock, or an FS/disk-full error) while upsert_baseline succeeds, so the cursor row exists with reviewed_sha=H0 but refs/myndaix/reviewed/<ref> is absent; (b) later the default branch is force-pushed off H0 (history rewrite); (c) the next advancing fetch's `+` refspec overwrites refs/myndaix/controller/<ref> from H0 to the new head H1, removing H0's last controller anchor; (d) once H0 is unreachable from all refs, falls out of reflog grace, and ages past gc.pruneExpire, a gc prunes it; (e) every subsequent tick computes base=reviewed_sha=H0, the cat-file gate misses, and the repo is skipped forever with no block/surface. I traced this in the live code and reproduced the git mechanics (force-push + `+`-refspec overwrite of the owned head ref; no reflog on myndaix refs).\n\nWHY NOT real=false (the skeptical counterweights, and why they don't kill it): At seed time H0 IS still anchored by the just-fetched controller head ref (head==H0 at first sight), so the seed-pin failure is masked initially and only becomes load-bearing on the next advancing fetch — but that is precisely the tick that needs base=H0, so the chain still closes. The strongest mitigant is that the watched repos are real working clones (repos.json points at /Users/stevenfernandez/code/active/myndaix-runtime, an actively-developed clone), where HEAD/heads/remotes reflogs normally pin H0 for ~30 days (gc.reflogExpireUnreachable default), making prune-before-recovery unlikely; and the trigger (transient update-ref failure) is itself rare. So real-world probability is LOW and the blast radius is one repo's proactive review going dormant (never a mutation/merge). That makes it arguably MINOR rather than MAJOR. But the question asked is whether it genuinely manifests in the code as written, defaulting to false unless a concrete failing path exists — and it does: the unguarded seed pin is a real, reproducible deviation from the very invariant the advance path enforces, with a permanent, self-healing-free wedge as the consequence. The proposed fix (guard the seed pin symmetrically — only seed if the pin succeeds, since head is still reachable via the just-fetched head ref so re-pin succeeds next tick — and make the dispatch-path cat-file miss fail-loud/re-fetch instead of a bare silent return) is correct and consistent with the codebase's own advance-path pattern. Reporting REAL."
        }
      },
      {
        "severity": "MAJOR",
        "title": "repos.json that is valid JSON but not an object (e.g. a top-level array) crashes every tick with an uncaught AttributeError, silently disabling the whole controller — violating the documented fail-soft contract",
        "location": "/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/controller.py:168-176",
        "why_real": "load_config() parses repos.json (line 168) then unconditionally calls raw.items() (line 176). The except clause (lines 169-172) catches only FileNotFoundError, json.JSONDecodeError, and OSError. A file whose top-level JSON is a list/string/number/null parses WITHOUT a JSONDecodeError, so raw.items() raises AttributeError (verified: `[]`, `\"hello\"`, `123`, `null`, `true` all raise 'X object has no attribute items'). load_config() is called in tick() at line 419, BEFORE acquire_lock() and outside the per-repo try/except, and tick()/main() do not catch it — so asyncio.run(tick()) propagates the traceback and the process exits nonzero on EVERY tick. A top-level array is a very plausible misconfiguration (writing repos as a JSON list instead of an object). The docstring at lines 165-166 explicitly promises 'A missing or malformed file logs and yields [] (the tick no-ops; never crash-loops launchd)', and DESIGN §4 lists 'repos.json missing/malformed -> log + exit 0'. This breaks that guarantee: instead of no-op-and-log, the controller hard-crashes and dispatches nothing, indefinitely and silently (visible only in controller.out), until a human notices. It does NOT crash-loop launchd (the plist has no KeepAlive, so a nonzero exit just waits for the next StartInterval) and cannot corrupt the cursor or leak the lock (the crash is before acquire_lock), so it is MAJOR, not BLOCKER — but it converts a recoverable config typo into a permanent, unobserved outage of the brain.",
        "fix": "After json.loads at line 168, validate the shape before iterating, e.g.: `if not isinstance(raw, dict): log(f\"repos.json is {type(raw).__name__}, expected object — skipping tick\"); return []`. Place it immediately after the try/except block (before the for loop at line 176). This restores the documented log-and-no-op behavior for the non-object-but-valid-JSON case, matching the existing JSONDecodeError handling.",
        "lens": "failure-modes",
        "verdict": {
          "real": true,
          "reasoning": "VERIFIED REAL against the actual code on branch northstar/controller-loop, reproduced end-to-end in the project's own .venv.\n\nCrash path (controller.py): load_config() line 168 does raw = json.loads(REPOS_JSON.read_text()). The except block (169-172) catches only FileNotFoundError, json.JSONDecodeError, and OSError. It then unconditionally iterates raw.items() at line 176. A repos.json whose top-level JSON is a list/string/number/null/bool is VALID JSON (no JSONDecodeError), so .items() raises AttributeError, which is uncaught.\n\nReproduced: I ran the real module via the project venv with MYNDAIX_REPOS_JSON pointing at a file containing '[{\"path\":\"/tmp/x\"}]'. `python -m runtime.controller tick` produced a traceback ending at controller.py:176 `for key, entry in raw.items()` -> AttributeError: 'list' object has no attribute 'items', and exited with code 1. load_config() in isolation also propagated the AttributeError. Confirmed [] , \"hello\", 123, null, true all trigger it.\n\nNo catch anywhere: load_config() is called in tick() at line 419, BEFORE acquire_lock() (424) and OUTSIDE the per-repo try/except (435-438). main() (447-451) does `return asyncio.run(tick())` with no try/except. log() (85-87) is a plain print and swallows nothing. controller-tick.sh uses set -euo pipefail + exec, so the nonzero Python exit propagates as the launchd job's exit.\n\nContract violated: docstring lines 165-166 promise \"A missing or malformed file logs and yields [] (the tick no-ops; never crash-loops launchd)\"; DESIGN doc line 118 lists \"repos.json missing/malformed -> log + exit 0 (never crash-loop launchd).\" The non-object-valid-JSON case does neither — it raises and exits nonzero, matching the reviewer's WHY.\n\nSeverity MAJOR (not BLOCKER) is correct: the plist (ai.myndaix.controller.plist.example) has StartInterval 3600 + RunAtLoad and NO KeepAlive, so a nonzero exit waits an hour for the next tick rather than tight-looping; and the crash is before acquire_lock(), so no lock leak / no cursor corruption — but it is a silent, recurring, total no-dispatch outage of the brain for a plausible config typo (writing repos as a JSON array instead of an object), surfacing only in controller.out.\n\nMinor caveat (does not change verdict): a top-level array is technically \"valid JSON,\" so it sits just outside the literal word \"malformed,\" but it is functionally a malformed config and defeats the clear intent of the existing JSONDecodeError/OSError fail-soft handling. The proposed one-line isinstance(raw, dict) guard after the try/except correctly restores log-and-no-op.\""
        }
      },
      {
        "severity": "MAJOR",
        "title": "Empty-diff commit on the watched ref burns dispatch budget and wedges the head to BLOCKED (~3h of retries + a false BLOCKED alert)",
        "location": "src/runtime/controller.py:361 / orchestrator/play-review.sh:245",
        "why_real": "controller dispatches whenever head != reviewed_sha (line 361). A tree-identical commit on main (git commit --allow-empty -m retrigger — the very retry incantation play-review's own SKIPPED note at line 208 suggests — or an empty merge / self-revert) has a distinct sha but `git diff base tip` (play-review.sh:244, two-arg form compares trees) is EMPTY, so play-review.sh:245 aborts (exit 0, NO done-<tip> marker — abort() never calls mark_done). The controller's FRONT-exits-0 contract means trigger_review returns True, so _charge_day() runs (controller.py:392) charging the daily budget; the head is never advanced and never delivered. claim_dispatch then blocks re-claim until PENDING_STALE (1h) each retry; after MAX_ATTEMPTS=3 the head is marked BLOCKED (~3h) and a spurious 'BLOCKED ... after 3 attempts' is surfaced. It self-heals only when a real commit lands on top. Net: a routine empty/retrigger push silently consumes per-tick + daily dispatch budget and emits a false BLOCKED for a non-bug. The controller cannot distinguish 'review failed' from 'nothing to review' — both are an exit-0 worker with no done marker.",
        "fix": "Before dispatching, have the controller short-circuit a base..head whose diff is empty: run `git diff --quiet <base> <head>` (rc 0 = no changes) and if empty, treat it as already-reviewed — advance the cursor (reviewed_sha=head, attempts=0) WITHOUT dispatching, instead of firing play-review. This skips the wasted dispatch, the budget charge, and the false BLOCKED, and keeps the cursor moving forward over no-op commits.",
        "lens": "play-review-integration",
        "verdict": {
          "real": true,
          "reasoning": "CONFIRMED REAL in the code as written. I traced the full failing path against the actual current code on northstar/controller-loop.\n\nThe dispatch gate (src/runtime/controller.py:361) is exactly `if head == cur[\"reviewed_sha\"]: return` — anything else falls through to dispatch. I grep'd the whole controller: there is NO empty-diff / `git diff --quiet` short-circuit anywhere; the only pre-dispatch object check is `cat-file -e` (existence), at lines 374-376. So a tree-identical commit (distinct SHA) on the watched ref reaches dispatch.\n\nConcrete scenario (cursor: reviewed_sha=R, pending=NULL; user runs the exact incantation play-review.sh:208 itself recommends, `git commit --allow-empty -m retrigger && git push`, producing head E with tree(E)==tree(R)):\n- Tick 1: head=E != R → claim_dispatch (postgres_store.py:884) sets pending=E, state=dispatching, attempts=1 → trigger_review pipes \"refs/heads/main E refs/heads/main R\" + argv `origin \"\"`.\n- play-review FRONT (lines 56-69): remotesha=R is a real commit → cat-file -e passes → base=R, detaches worker, FRONT exits 0 UNCONDITIONALLY.\n- trigger_review sees rc==0 → returns True → budget[0]+=1 and _charge_day() (controller.py:390-392). DAILY/PER-TICK BUDGET CHARGED.\n- Worker: `git diff R E` (play-review.sh:244) is a two-arg TREE comparison; tree(E)==tree(R) ⇒ empty output ⇒ line 245 `abort diff \"empty/failed diff\"`. abort() (line 108) delivers an ABORTED note and `exit 0` BEFORE ever reaching confirm_pushed (line 280) or any mark_done (line 148). So NO done-E marker is written, and `pushed` is never set.\n- Subsequent ticks: advance pass (controller.py:328-337) needs _done(E) → false, never advances. claim_dispatch's WHERE (postgres_store.py:909) re-claims the same pending head only once updated_at < stale_before (PENDING_STALE=3600), incrementing attempts each stale re-claim. After attempts hits MAX_ATTEMPTS=3, the ceiling at controller.py:365-368 fires mark_blocked and logs \"BLOCKED ... after 3 attempts — surfaced\". \n\nNet effect exactly as the reviewer claims: a routine empty/retrigger push (a) burns per-tick + daily dispatch budget on every re-dispatch, (b) never advances the cursor, (c) wedges the head to BLOCKED after ~3 stale retries (~3h with hourly ticks + 1h stale), (d) emits a spurious BLOCKED alert for a non-bug. It self-heals only when a real commit lands (distinct head with non-empty tree resets attempts to 1, escaping blocked).\n\nThe controller genuinely cannot distinguish \"review failed\" from \"nothing to review\": both are an exit-0 FRONT with no done-<sha> marker, and it charges budget on the former. No test covers this (grep of tests/ found nothing for empty-diff/allow-empty/noop commits). The proposed fix (a `git diff --quiet base head` short-circuit that advances the cursor to head without dispatching) is sound and mirrors the controller's existing object-check pattern.\n\nSeverity nuance: impact is BOUNDED and self-healing (read-only, ~3 wasted dispatch slots, one false alert), so I'd lean MINOR-to-MAJOR rather than a hard MAJOR — but the defect itself (false BLOCKED + wasted budget on system-recommended user behavior) is genuine and currently present."
        }
      },
      {
        "severity": "MINOR",
        "title": "Stale comment references the deleted PLAY_FORCE_DONE flag as the branch-move-proof mechanism",
        "location": "src/runtime/controller.py:327",
        "why_real": "Line 327 reads 'done-<sha> marker, made branch-move-proof by PLAY_FORCE_DONE'. PLAY_FORCE_DONE was DELETED in v0.5 (docs/controller-loop-design.md:8); the branch-move-proof property now comes from the empty-URL path making confirm_pushed return 0 -> pushed=1 -> mark_done writes unconditionally. The code is correct; the comment misdescribes the live mechanism and will mislead the next reader debugging the advance path. (Line 130's 'WITHOUT a public PLAY_FORCE_DONE bypass' is accurate — it describes what is deliberately NOT used.)",
        "fix": "Update controller.py:327 to: 'done-<sha> marker, made branch-move-proof by the empty-URL path (confirm_pushed returns 0 -> mark_done writes unconditionally)'.",
        "lens": "play-review-integration",
        "verdict": {
          "real": true,
          "reasoning": "Verified on branch northstar/controller-loop. The claim is factually correct on every point.\n\n1) The stale comment exists verbatim. src/runtime/controller.py:327 reads: \"# done-<sha> marker, made branch-move-proof by PLAY_FORCE_DONE) moves the cursor.\" (part of the advance-pass comment block at lines 326-327 in process_repo).\n\n2) PLAY_FORCE_DONE is genuinely gone from live code. A repo-wide grep finds PLAY_FORCE_DONE only in: docs/controller-loop-design.md (the v0.5 \"DELETED\" changelog line 8 + v0.4 historical line 16), two historical review files under docs/reviews/, and two comments in controller.py (line 130 and line 327). It appears NOWHERE in orchestrator/play-review.sh — confirm_pushed (line 128), mark_done (line 148), and autofix_armed (line 162) make no reference to it. So the flag the line-327 comment names as the active branch-move-proof mechanism no longer exists.\n\n3) The actual live mechanism is the empty-URL path, exactly as the proposed fix states. controller.py:296 passes argv [\"origin\", \"\"] (empty remote URL) with synthetic-stdin; play-review.sh:128-129 `confirm_pushed(){ [[ -z \"$remote_url\" ]] && return 0 ...}` returns 0 for the empty URL (no ls-remote), which sets pushed=1, and play-review.sh:148 `mark_done(){ [[ \"${pushed:-0}\" == \"1\" ]] && : > \"$STATE/done-$tip\" ... }` then writes the done marker unconditionally. That is what makes the controller's advance branch-move-proof now.\n\n4) The reviewer's note about line 130 is also correct: \"writes the post-delivery done-<sha> marker WITHOUT a public PLAY_FORCE_DONE bypass\" accurately describes what is deliberately NOT used; only line 327 is wrong.\n\nSkeptic's caveat: this is a documentation-only defect. It causes NO runtime misbehavior — the advance path is correct and the reviewer concedes \"The code is correct.\" There is no failing execution path to construct; nothing in output/state is wrong. So it does not \"manifest\" at runtime. But it is a real, current, verifiable inaccuracy in the source as written: a code comment that names a deleted flag as the live mechanism, which would genuinely mislead the next person debugging the advance path. Correctly classified MINOR. Calling it real=true on the basis that it is a genuine, present, accurately-located defect in the current code with an accurate proposed fix; it is correctly scoped as cosmetic/maintainability, not functional.\n\nRelevant files: /Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/controller.py (lines 326-327 stale comment, line 130 accurate comment, line 296 empty-URL argv), /Users/stevenfernandez/code/active/myndaix-runtime/orchestrator/play-review.sh (lines 128-129 confirm_pushed, line 148 mark_done), /Users/stevenfernandez/code/active/myndaix-runtime/docs/controller-loop-design.md (line 8 v0.5 PLAY_FORCE_DONE DELETED)."
        }
      }
    ],
    "dismissed_count": 2,
    "dismissed_titles": [
      "state-machine: Stale-pending re-claim uses the controller host clock for stale_before while updated_at is written by the DB now(); clock skew shifts the dead-dispatch window",
      "play-review-integration: Controller execs the INSTALLED $ORCH/play-review.sh, which currently predates PLAY_DISABLE_AUTOFIX; the controller cannot verify the guard is present"
    ],
    "completeness_gaps": {
      "findings": [
        {
          "severity": "BLOCKER",
          "title": "The synthetic-stdin + done-marker advance loop is never tested end-to-end — the core contract is two unjoined halves",
          "location": "tests/test_controller.py:114-156; orchestrator/test.sh:54,107-108",
          "why_real": "controller.py advance (process_repo lines 328-337) advances the cursor ONLY when play-review writes STATE/done-<pending_sha>. play-review writes that marker only via mark_done (play-review.sh:148), which requires pushed=1, which under the controller's empty-URL invocation comes from confirm_pushed returning 0 (play-review.sh:128-129). NO test feeds the controller's actual line `<ref> <head> <ref> <base>` + argv `origin \"\"` into a real play-review and then checks a done-<head> file appears AND the next process_repo advances. test_done_marker_advances_cursor FAKES the marker with a manual write (line 131); test_trigger_failure_releases runs the real trigger but against a MISSING script (line 146), so only the failure branch is covered; orchestrator/test.sh drives the WORKER directly via --worker and one FRONT case with remotesha=0000 (a delete-style line, NOT the controller's reviewed_sha line). So if play-review's FRONT base computation, the worker mark_done gate, or the STATE path ever drifts from what the controller expects, every test stays green while the cursor never advances and the repo is re-reviewed every tick forever — the exact v0.3 'branch-move re-review forever' failure class, now uncovered.",
          "fix": "Add one integration test that installs a real (or thin-stub) play-review.sh at PLAY_REVIEW, runs process_repo with TEST_MODE off against a temp repo whose HEAD advanced, asserts STATE/done-<head> is created, then runs process_repo again and asserts the cursor advanced to head with state='delivered'. Cheaper alternative: have the human run docs §8a steps 3-5 (dry-run, seed tick, post-push tick + advance) on a live repo and record the review_cursor transitions before merge."
        },
        {
          "severity": "MINOR",
          "title": "Stale comment asserts the deleted PLAY_FORCE_DONE mechanism still drives advance",
          "location": "src/runtime/controller.py:327",
          "why_real": "v0.5 DELETED PLAY_FORCE_DONE (design changelog line 8) and replaced it with the empty-URL path. But the advance-pass comment still reads '...post-delivery done-<sha> marker, made branch-move-proof by PLAY_FORCE_DONE) moves the cursor.' The code itself is correct (it relies on the empty-URL confirm_pushed path, not the deleted flag), but the comment documents a mechanism that no longer exists, and it is precisely the branch-move-proofness claim a reviewer is asked to verify. controller.py:130 and :278 already say the right thing; only :327 is stale.",
          "fix": "Reword controller.py:327 to drop PLAY_FORCE_DONE, e.g. '...post-delivery done-<sha> marker (the controller passes an empty remote URL so confirm_pushed treats the dispatch as pushed and writes it) moves the cursor.'"
        },
        {
          "severity": "MINOR",
          "title": "Daily dispatch budget counter is untested and is a non-atomic read-modify-write",
          "location": "src/runtime/controller.py:403-415,427-433",
          "why_real": "_charge_day does _day_file().write_text(str(_day_count()+1)) — a non-atomic read-modify-write on STATE/controller-day-<UTCday>. No test touches _day_count/_charge_day/MAX_DISPATCH_PER_DAY or the tick()-level day-cap gate (grep of test_controller.py returns nothing). The design (§4 'Runaway dispatch', §7 knobs) asserts a daily budget bounds dispatch; that assertion is unverified by code. Concurrency risk is low because acquire_lock (flock) makes the tick single-instance, but the cap is a stated safety bound shipping with zero tests. Separately, the controller's controller-day-<day> file is distinct from play-review's count-<day> (play-review.sh:233), so a brain dispatch does not charge play-review's daily cap and vice-versa — intentional but undocumented and untested.",
          "fix": "Add a test that sets MYNDAIX_CONTROLLER_MAX_DAY low, pre-writes the controller-day file near the cap, and asserts tick()/process_repo stops dispatching and logs 'observing only'. Optionally make _charge_day atomic (write temp + os.replace), and note in docs that controller and play-review keep independent daily counters."
        },
        {
          "severity": "MINOR",
          "title": "SSH-auth and env inheritance under launchd is a live-only assumption the plist does not satisfy",
          "location": "orchestrator/ai.myndaix.controller.plist.example:14-30; src/runtime/controller.py:118-120,141-143",
          "why_real": "_git_env/_review_env forward SSH_AUTH_SOCK only `if os.environ.get(k)`. The example plist sets NO EnvironmentVariables block, and the live ai.myndaix.fix-sweep.plist it claims to mirror also sets none (verified on disk). launchd user-agents do not reliably inherit an interactive shell's ssh-agent socket, so a repo whose origin is ssh:// or git@ will, under the hourly launchd tick, fetch with no SSH_AUTH_SOCK and GIT_TERMINAL_PROMPT=0 — the fetch fails fast and process_repo silently skips that repo every tick (fail-closed, not a hang, but it never reviews and only logs 'fetch failed'). The test suite uses file:// remotes exclusively, so this never surfaces in CI; it can only be confirmed live. The design §5/§8a does not flag it.",
          "fix": "Before merge, run the controller once under the real launchd agent (launchctl load then launchctl kickstart -k) against a repo with an SSH remote and confirm the fetch succeeds — or add an EnvironmentVariables block with SSH_AUTH_SOCK to the plist, or document in §8a that ssh-remote repos require the launchd session to have ssh-agent access."
        }
      ],
      "summary": "Completeness review of the controller-loop on northstar/controller-loop, final pre-merge. The code is logically sound and well-covered at the unit level (cursor CAS verbs in test_postgres_ledger.py; the process_repo decision matrix across 14 cases in test_controller.py). The dominant gap is structural: the single most load-bearing integration claim — the synthetic-stdin contract where the controller pipes \"<ref> <head> <ref> <reviewed_sha>\" + argv `origin \"\"` into play-review, play-review computes base=reviewed_sha, delivers, and writes the done-<sha> marker the controller then reads to advance the cursor — is NEVER exercised end to end. Every controller test substitutes the TEST_MODE dispatch seam; the one real-path test points PLAY_REVIEW at a missing script (failure branch only); play-review's own test.sh drives the worker directly and never the controller's exact empty-URL FRONT invocation. So the advance loop that prevents re-review-forever is asserted by two independent halves that no test joins — the exact v0.3 failure class. Three smaller items: a stale comment citing the deleted PLAY_FORCE_DONE (controller.py:327), an untested non-atomic daily-budget counter, and an SSH-auth-under-launchd assumption the example/live plists do not satisfy and only a live run can confirm. None are code-quality defects — they are verification/coverage/integration gaps the human should clear before merge, most cheaply by running the doc's §8a dry-run + two-tick manual runbook on a real repo (ideally one with an SSH remote).</parameter>\n</invoke>\n"
    }
  }
}