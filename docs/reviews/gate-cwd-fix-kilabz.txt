**Findings**

- **Medium:** [runner.py:154](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/runner.py:154) uses `tempfile.mkdtemp()` with the process temp root, and [runner.py:44](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/runner.py:44) also passes `TMPDIR` through to the child. If `TMPDIR` resolves under the serve repo, the agent runs inside a subdir of the base checkout; `git` parent discovery / `..` reads can still see the wrong tree. The test at [test_runner.py:63](/Users/stevenfernandez/code/active/myndaix-runtime/tests/test_runner.py:63) only proves `cwd != serve_cwd`, not “not under serve_cwd”. I’d validate `realpath/commonpath` or create the scratch cwd under a known out-of-repo root, and override child `TMPDIR` to the scratch temp area.

- **Low:** [runner.py:154](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/runner.py:154) allocates `scratch_cwd` before the `try/finally` starts at [runner.py:156](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/runner.py:156). If `mkdtemp()` raises, `invoke_cli` bubbles an exception instead of returning a `Result`; if `_make_scratch_home()` already created a scratch home, that can leak. Initialize temp paths to `None`, enter the `try`, then allocate/map `OSError` to a clean retryable error.

- **Low:** [runner.py:153](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/runner.py:153) checks only `cwd is None`, but [runner.py:163](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/runner.py:163) passes `cwd or scratch_cwd`. If `worktree_path == ""`, no scratch cwd is made and `cwd=None` is passed, reintroducing inherited cwd. Not reachable in the normal worker path, but easy to harden with `cwd = job.worktree_path or None`.

I don’t see a legitimate current roster agent that should keep the repo cwd. `kilabz`, `oracle`, and `lobster` receive fully inlined prompt data from `play-review.sh`; workspace actors still get their explicit worktree. Manual `mxr codex|mack|mini ...` without `--repo` may change behavior, but that was relying on the accidental serve cwd.

Empty cwd is sufficient for the phantom-triage fix only if the scratch cwd and inherited env do not expose the base checkout. The prompt path looks clean: `play-review.sh` passes repo id/base ref metadata, not the absolute repo path. I did not run the tests because this session’s filesystem sandbox is read-only and these tests create temp dirs.
