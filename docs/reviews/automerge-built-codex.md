**Verdict: NEEDS-REVISION**

Do not arm `AUTOMERGE_ENABLED` yet. The REST merge primitive is the right one (`sha=H`, `merged == true`), and the normal `.py`/rename-to-code paths are rejected, but the hardened v0.3 contract is not met in several load-bearing places.

**BLOCKER**
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:128): `classify_diff()` is not a closed security boundary. It accepts unsupported statuses and unsafe old-side modes as long as `nmode == 100644`; direct probes show `T 120000->100644 link.md`, `T 100755->100644 run.md`, `C100 120000->100644 old.md new.md`, and even status `X` all return `(True, "docs-only")`. Fix: whitelist statuses `A/M/D/R*/C*`; validate both modes where present; reject `T/U/X/B`; for `A` require `000000->100644`, `M/R/C` require `100644->100644`, `D` require `100644->000000`. Add tests for these.
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:99): `parse_raw_z()` silently skips malformed/truncated tokens and `classify_diff()` can still pass earlier safe entries. That is the wrong failure mode for the stated security boundary. Fix: make parse strict: raise/return invalid on unexpected token, bad field count, truncated path list, invalid UTF-8, or trailing garbage; caller must reject parse errors.

**MAJOR**
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:258): CI gate matches only check-run name `"test"`, not exact app/workflow identity, ignores legacy statuses, allows duplicate successful ambiguous providers, and `gh api --paginate` without `--slurp` can emit multiple JSON pages that `_gh_json()` cannot parse. Fix: query check-runs plus statuses for `H`, use `--slurp` or REST pagination handling, pin expected check identity, reject zero/running/failing/duplicate-ambiguous providers.
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:360): after the long review window, the code does not re-query `headRefOid == H`, `baseRefOid == M`, `mergeStateStatus`, merge queue, or CI before merging. `sha=H` closes the head move, but v0.3 requires the full just-before-merge recheck. Fix: immediately before the PUT, re-read PR metadata/rules/checks and fail closed unless all pinned values still match.
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:334): fetch does not pin `refs/pull/<n>/head` into an owned ref and does not assert fetched OID equals `H`. The raw SHA fetch is not portable, and the fallback fetches only `main`. Fix: fetch `+refs/pull/<n>/head:refs/automerge/pr/<n>` and `+refs/heads/main:refs/automerge/main`, then `rev-parse` and assert exact `H/M`.
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:275): verdict freshness is weaker than v0.3: predictable `automerge-verdict-{H}.json`, deterministic `run_id`, unlink errors ignored, and subprocess return code ignored. A stale PASS can validate if cleanup/write fails. Fix: per-attempt random run id, fresh `0700` temp dir, fail if verdict path cannot be created/removed, require `returncode == 0` and matching `{run_id,B,H,PASS}`.
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:247): cap accounting fails open on state I/O errors; `_count()` returns `0` on read errors and `_charge()` swallows write errors after merging. Also per-author cap returns `"skipped"` and is recorded, so the same head will never merge on a later day. Fix: caps should be enforced in durable storage or fail closed before merge; cap deferrals should return `None`, not recorded terminal decisions.

**MINOR**
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:200): `gh repo view --json nameWithOwner -q .nameWithOwner` emits a plain string, but `_gh_json()` requires JSON. `load_repo()` likely never resolves `nwo`. Fix: do not use `_gh_json()` for raw `--jq` strings, or output JSON and parse that.
- [src/runtime/automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/automerge.py:394): rate-limit preflight proceeds when the query is unparsable or fails. Fix: `rl is None` should fail closed.
- [tests/test_automerge.py](/Users/stevenfernandez/code/active/myndaix-runtime/tests/test_automerge.py:35): tests miss the failing mode/status/parser cases above and do not cover CI identity, pre-merge recheck, stale verdict, or cap deferral.

I ran `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python tests/test_automerge.py`; it passes, but the added direct probes expose the classifier holes. `git diff --check` also reports trailing whitespace in `docs/reviews/automerge-design-v0.2-oracle.md`.
