**Verdict: NEEDS-REVISION**

v0.2 folded the right attack classes, but not yet with enough precision for a first human-gate removal.

**BLOCKER Findings**
- [docs/automerge-design.md](/Users/stevenfernandez/code/active/myndaix-runtime/docs/automerge-design.md:40): `play-review --gate` is feasible, but the design is not specific enough to be safe. Current [play-review.sh](/Users/stevenfernandez/code/active/myndaix-runtime/orchestrator/play-review.sh:44) treats anything except `--worker` as hook FRONT, detaches, and exits 0; abort/contention also exit 0, Oracle is best-effort, and `done-<sha>` is PASS-blind. Fix: define a real CLI contract before FRONT parsing, e.g. `--gate <repo> <base> <tip> <ref> <run-id> <verdict-path>`, run inline, make Oracle required for automerge, make every abort/skip/contention nonzero, never write `done-*`, never fire autofix, and write only to a fresh 0700 run dir owned by the automerge tick. The verdict file must include and validate `{run_id, base, head, verdict}`; `verdict-<H>` alone is stale/replay-prone.

- [docs/automerge-design.md](/Users/stevenfernandez/code/active/myndaix-runtime/docs/automerge-design.md:25): merge-queue refusal is conceptually right but underspecified. `gh pr merge` explicitly queues/enables deferred merge on queue-protected branches, and `--match-head-commit` does not change that behavior. The design needs a concrete preflight: `gh api repos/{owner}/{repo}/rules/branches/main --jq 'any(.[]; .type=="merge_queue")'` and fail closed if true. GitHub’s rules API says branch rules return active rules for the branch, and `merge_queue` means merges must go through a queue. Sources: GitHub CLI merge docs and GitHub rules API. ([docs.github.com](https://docs.github.com/en/rest/rate-limit/rate-limit?apiVersion=2022-11-28)) ([docs.github.com](https://docs.github.com/en/rest/repos/rules?apiVersion=2022-11-28))

**MAJOR Findings**
- [docs/automerge-design.md](/Users/stevenfernandez/code/active/myndaix-runtime/docs/automerge-design.md:24): B2 pins `H` but not the base actually merged. `--match-head-commit H` is correct for head TOCTOU; GitHub documents this as requiring the PR head to match, and REST returns 409 if `sha` mismatches. But it does not bind `main`/`baseRefOid`. Fix: pin `M = baseRefOid` or fetched `origin/main` OID, compute `B = merge-base(M,H)`, use `B..H` everywhere, then immediately before merge re-query `headRefOid == H`, `baseRefOid == M` or intentionally accept/recompute, `mergeStateStatus == CLEAN`, no queue, checks still green. Prefer REST `PUT /pulls/{n}/merge` with `sha:H` so the response gives structured `{merged, sha}`. ([docs.github.com](https://docs.github.com/en/rest/rate-limit/rate-limit?apiVersion=2022-11-28)) ([docs.github.com](https://docs.github.com/en/rest/repos/rules?apiVersion=2022-11-28))

- [docs/automerge-design.md](/Users/stevenfernandez/code/active/myndaix-runtime/docs/automerge-design.md:37): the denylist is not concrete enough for this repo. I found no tracked `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `rules/*.md`, or `SKILL.md`, and no runtime code that literally reads Markdown prompt files. But this repo does use Markdown design/spec/runbook files as agent operational ground truth. At minimum route these to human: `DESIGN.md`, `docs/orchestrator-design.md`, `docs/controller-loop-design.md`, `docs/phase2-autonomous-fix-flip-design.md`, `docs/phase2-pr4-fix-stage-design.md`, `docs/phase2-pr4-fix-stage-spec.md`, `docs/automerge-design.md`, `docs/OPERATING.md`, plus future `**/SKILL.md`, `**/.agents/**`, `.codex/**`, `.claude/**`, `.cursor/rules/**`, `.github/**`.

- [docs/automerge-design.md](/Users/stevenfernandez/code/active/myndaix-runtime/docs/automerge-design.md:34): `git diff --raw -z -M` is the right sole source, but deletion handling is ambiguous. A deleted `.md` has destination mode `000000`, contradicting “destination mode is regular blob `100644`.” Fix: either explicitly reject deletes, or allow status `D` only when old mode/path are safe and new mode is `000000`. Also require `--no-ext-diff`, scrub git env like [controller.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/controller.py:110), and reject `100755` outright.

- [docs/automerge-design.md](/Users/stevenfernandez/code/active/myndaix-runtime/docs/automerge-design.md:16): environmental blockers are incomplete. Add: exact token permissions (`GH_TOKEN`/`GITHUB_TOKEN`; fine-grained PAT needs contents/pull-request/checks metadata permissions, not “repo” scope), launchd env/PATH/DSN, `mxr` plus kilabz/lobster/oracle creds under launchd, trusted installed `play-review.sh` checks like [controller.py](/Users/stevenfernandez/code/active/myndaix-runtime/src/runtime/controller.py:242), GitHub rate-limit preflight, and fetch strategy for PR heads. `git fetch H` by raw SHA is not portable enough; fetch `refs/pull/<n>/head` into an owned ref and assert it equals `H`, or reject cross-repo PRs for v1. GitHub exposes rate status explicitly; use it and fail closed below a threshold. ([docs.github.com](https://docs.github.com/en/rest/rate-limit/rate-limit?apiVersion=2022-11-28))

**MINOR Findings**
- [docs/automerge-design.md](/Users/stevenfernandez/code/active/myndaix-runtime/docs/automerge-design.md:27): status parsing should require an exact expected check identity, not “every `test` entry.” Match exact name plus app/workflow identity where available; reject duplicate ambiguous providers.

- [docs/automerge-design.md](/Users/stevenfernandez/code/active/myndaix-runtime/docs/automerge-design.md:29): `reviewDecision == APPROVED` is fine only after parsing active branch rules. Use branch rules `pull_request.required_approving_review_count`, code-owner/required-reviewer fields, and fail closed on unknown.

Net: head-match merge semantics are right; raw diff-class direction is right; author allowlist and caps are right. The remaining holes are integration contracts: gate verdict freshness, concrete merge-queue detection, base pinning, and repo-specific instruction-doc denylist.
