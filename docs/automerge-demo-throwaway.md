# Auto-merge gate demonstration (throwaway)

This is a deliberately trivial, self-contained docs file created to exercise the
docs-only auto-merge gate end-to-end after the reviewer-cwd fix (PR #39). It states
no operational facts, contradicts nothing, and is safe to delete once the gate
demonstration is complete.
